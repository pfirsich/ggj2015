-- This is lua source code (if your editor supports it, turn syntax highlighting on!)
return {
	width = 1024,				-- The window width (number)
	height = 768,				-- The window height (number)
	vsync = false,				-- Enable vertical sync (true/false)
	fullscreen = false,		-- Enable fullscreen (true/false)
	display = 1,				-- Index of the monitor to show the window in (number)
	highdpi = false,			-- Enable high-dpi mode for the window on a Retina display (true/false).
	defaultVolume = 0.3,
	
	players = {
		--{color = {255, 0, 0}, female = false, controller = joystickController(1)},
		{color = {0, 255, 0}, female = false, controller = keyboardController("left", "right", "up", " ", "lalt")}
	}
}